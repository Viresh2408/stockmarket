# ── Python ───────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/
.env

# ── Data files ───────────────────────────────────────────────────
# Raw download — too large and regeneratable
data/raw_prices.csv
# Keep the clean dataset (imported directly into Power BI)
!data/stock_data_clean.csv

# ── Power BI ─────────────────────────────────────────────────────
*.pbix.tmp
*.pbit
*.pbihash
*.tmp
# Keep the .pbix if saved here
!dashboard/stock_risk_dashboard.pbix

# ── Dashboard screenshots ─────────────────────────────────────────
# Keep — referenced in README.md and dashboard/README.md
!dashboard/screenshots/

# ── Streamlit cache ───────────────────────────────────────────────
.streamlit/
__streamlit_cache__/

# ── OS files ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Exclude old Next.js artefacts (already deleted locally) ──────
node_modules/
.next/
out/
build/
